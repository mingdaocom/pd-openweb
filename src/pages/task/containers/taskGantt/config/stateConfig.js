export default {
  // 当前任务状态
  currentStatus: -1,

  // 当前视图
  currentView: 1,

  // 是否过滤周末
  filterWeekend: true,

  // 当前展开层级
  currentLevel: 0,

  // 拖拽 taskId
  dragTaskId: '',

  // 拖拽 经过的元素块下标
  dragHoverIndex: -1,
};
