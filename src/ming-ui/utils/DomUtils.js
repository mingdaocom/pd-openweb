/**
 * 获取元素某个样式
 * @param {目标元素} element
 * @param {属性} attr
 */
/**
 * 获取元素某个样式
 * @param {目标元素} element
 * @param {属性} attr
 */
/**
 * 获取元素某个样式
 * @param {目标元素} element
 * @param {属性} attr
 */
/**
 * 获取元素某个样式
 * @param {目标元素} element
 * @param {属性} attr
 */
export const getComputedStyle = (element, attr) => {
  return window.getComputedStyle(element, null).getPropertyValue(attr);
};
